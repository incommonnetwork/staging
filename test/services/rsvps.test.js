// const app = require('../../server/app');

describe('\'rsvps\' service', () => {
    it('registered the service', () => {
        // const service = app.service('rsvps');
        // expect(service).toBeTruthy();
    });
});
